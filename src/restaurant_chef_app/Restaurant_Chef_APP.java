/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package restaurant_chef_app;

import restaurant_chef_app.clases.*;

import java.util.ArrayList;
import java.util.List;
import restaurant_chef_app.Controller.PedidoController;
/**
 *
 * @author Sebastian
 */
public class Restaurant_Chef_APP {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
       /*// Cargar pedidos existentes
        List<Pedido> listaPedidos = PedidoController.cargarPedidos();

        // Crear empleado
        Empleado empleado = new Empleado("E001", "Carlos", "1234");

        // Crear pedido nuevo
        Pedido nuevoPedido = new Pedido("P003", "Luis Gómez");
        nuevoPedido.agregarPlatillo(new Platillo("Perro caliente", 10000,"comidas"));
        nuevoPedido.agregarPlatillo(new Platillo("Jugo de mora", 4000,"bebidas"));

        // Empleado toma el pedido
        empleado.tomarPedido(nuevoPedido, listaPedidos);

        // Luego lo envía a cocina
        empleado.enviarPedidoACocina(nuevoPedido, listaPedidos);

        // Cargar lista actualizada
        List<Pedido> pedidosActualizados = PedidoController.cargarPedidos();

        System.out.println("\n Pedidos guardados:");
        for (Pedido p : pedidosActualizados) {
            System.out.println("- " + p.getId() + " | Cliente: " + p.getNombreCliente() + " | Estado: " + p.getEstado());
        }
*/
    }
    
}
